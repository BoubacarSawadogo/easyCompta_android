<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.easycompta">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logoeasycompta"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.BanqueActivity"></activity>
        <activity android:name=".activities.BeneficeActivity" />
        <activity android:name=".activities.StockActivity" />
        <activity android:name=".activities.ListeDepensesActivity" />
        <activity android:name=".activities.ListeVentesActivity" />
        <activity android:name=".activities.SaisieDepenseFormActivity" />
        <activity android:name=".activities.SaisieVenteFormActivity" />
        <activity android:name=".activities.EtatsActivity" />
        <activity android:name=".activities.SplasScreenActivity" />
        <activity android:name=".activities.WelcomeActivity" />
        <activity android:name=".activities.MainRegister" />
        <activity android:name=".activities.MainPassWordReset" />
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>